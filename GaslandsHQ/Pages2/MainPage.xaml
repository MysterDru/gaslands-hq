<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="GaslandsHQ.Pages2.MainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="Add Team"
            Command="{Binding AddTeam}" />
    </ContentPage.ToolbarItems>
    <ListView
        x:Name="TeamList"

        HasUnevenRows="true"
        ItemsSource="{Binding Teams}"
        ItemSelected="TeamList_ItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid
                        Padding="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" Text="{Binding TeamName}"
                               VerticalTextAlignment="Center"
                                FontAttributes="Bold"/>
                        <Label Grid.Column="1" Text="{Binding TotalCans, StringFormat='{0} Cans'}"
                               VerticalTextAlignment="Center"/>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>
</ContentPage>
